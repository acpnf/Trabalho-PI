Menu Inicial do Jogo — CINDEFENDA

Este diretório contém a implementação do menu principal do jogo CINDEFENDA, utilizando a biblioteca raylib em C.
    
🎮 Funcionalidades:

    Tela de menu principal com botões:

        Iniciar (sem funcionalidade por enquanto)

        Sair do jogo

        Ícone de configurações no canto superior direito

    Tela de configurações com:

        Controle de áudio (Som ON/OFF)

        Alternância entre janela e tela cheia

        Botão Voltar para retornar ao menu principal

    Música de fundo (formato .wav) com controle de play/pause

    Imagens integradas:

        menu.png: plano de fundo

        configuracao.png: ícone de engrenagem

        pergaminho.png: painel visual da tela de configurações

🎵 Áudio:

A música de fundo está localizada em /menu/Som/menu.wav e é gerenciada através das funções:

InitAudioDevice();
LoadMusicStream(...);
PlayMusicStream(...);
UpdateMusicStream(...); // chamada a cada frame
StopMusicStream(...);

A música continua tocando enquanto o jogador estiver navegando pelo menu, e pode ser pausada nas configurações.

🖼️ Imagens:

As imagens utilizadas estão na pasta Imagens/, com caminhos absolutos atualmente definidos no código. É recomendado ajustar os caminhos para relativos antes de distribuição (ex: ./Imagens/menu.png).

⚙️ Requisitos

    Compilador C (GCC ou compatível)

    raylib instalada (com suporte a áudio)

    Arquivos de imagem e som nos diretórios corretos

💡 Compilação (exemplo com gcc)

gcc menu.c -o menu -lraylib -lGL -lm -lpthread -ldl -lrt -lX11

    Certifique-se de que os arquivos .wav e .png estejam no local certo ou ajuste os caminhos no código.
